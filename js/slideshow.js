jQuery(document).ready(function(){
		jQuery('#slideShowContent').slick({
			autoplay: true,
			autoplaySpeed: 4000,  
			infinite: true,
			speed: 400,
			fade: true,
			cssEase: 'linear',
			dots:true,
			arrows:false
	});
});
